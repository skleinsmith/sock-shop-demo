apiVersion: v1
kind: Service
metadata:
    annotations:
        harness.io/managed: false
    name: {{ .Values.name }}
    labels:
spec:
    ports:
    # the port that this service should serve on
    - port: 27017
      targetPort: 27017
      selector:
      name: {{ .Values.name }}